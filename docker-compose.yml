version: '3.8'

services:
  web:
    build: .
    container_name: prepai_app
    ports:
      - "5000:5000"
    env_file:
      - .env
    volumes:
      - ./uploads:/app/uploads
      - ./interview_prep.db:/app/interview_prep.db
    restart: unless-stopped
